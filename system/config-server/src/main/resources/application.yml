info:
  component: Micro Porject Cloud Configuration Server
  
server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git :
          uri: https://github.com/ahkhatta/spring-cloud-configuration
logging:
  level:
    ROOT: INFO
    org.springframework.web: DEBUG
